class SearchController < ApplicationController
  #before_action :authenticate_user!
  def search
    # @search = Post.ransack(params[:q])
    # @search_posts = @search.result.page(params[:page])
    # @sentence = params[:centence]
  end
  #@posts = Post.all
end